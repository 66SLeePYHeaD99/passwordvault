
name: password_vault_app
description: A password vault app with face recognition, SQLite, and secure storage.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^5.0.2
  random_password_generator: ^0.2.1
  local_auth: ^2.1.3  # Add this for biometric authentication
  shared_preferences: ^2.0.8  # Add this for local storage
  sqflite: ^2.0.3+1
  path_provider: ^2.0.10
  



flutter:
  uses-material-design: true

  